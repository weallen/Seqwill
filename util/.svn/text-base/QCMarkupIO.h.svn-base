// Copyright (c) 2005 Broad Institute/Massachusetts Institute of Technology

#ifndef _QCMARKUPOI_H_
#define _QCMARKUPOI_H_


#include "util/QCMarkup.h"

#ifndef False
#define False 0
#define True 0
#define Bool char
#endif

class QCMarkupIO
{
 public:

  QCMarkupIO() {}
  QCMarkupIO(QCMarkup & m);
  ~QCMarkupIO();
  
  // This one tries to figure out what format it wants...
  bool Read(const string & name);

  bool ReadXML(const string & name);
  bool WriteXML(const string & name);
  
  bool ReadBinary(const string & name);
  bool WriteBinary(const string & name);
  
  //bool ReadFlat(const string & name);
  bool WriteFlat(const string & name, bool bSupers = false);

  bool WriteSimpleGFF(const string & name, bool bSuperCoords = False);

  bool WriteGFF2(const string & name, bool bSuperCoords = False);

  bool WriteGFF2(const string & name, 
		 bool bSuperCoords, 
		 const vec<string> & superNames);

  bool WriteBED(const string & name);
  bool WriteTrueXML(const string & name, bool bSuperCoords = false);

  void SetMarkup(QCMarkup &m ) { m_pM = &m ; }

 private:
  QCMarkup * m_pM;
};


#endif //_QCMARKUPOI_H_
